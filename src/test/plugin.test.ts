import { expect, it, describe, beforeEach, afterEach } from 'vitest';
// import plugin from '../index';

// @ts-ignore
// import config from './catalog/eventcatalog.config';

describe('AsyncAPI EventCatalog Plugin', () => {
  describe('service generation', () => {
    it('given an AsyncAPI spec it generated a new service into the catalog', async () => {
      // plugin(config, { path: 'catalog/eventcatalog.config' });
      expect(true).toBe(true);
    });
  });
});
